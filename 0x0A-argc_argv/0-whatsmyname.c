#include <stdio.h>
/**
 * main - Prints program name
 * @argc: number of arguments
 * @argv: Carries the program name
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
