#include <stdio.h>
/**
 * main - print the first 50 fibonacci
 * return: always 0
 */
int main(void)
{
	int i;
	int n;

	for (i = 1 ; i <= 50 ; i++)
	{
		for (n = i + n)
	}
	printf("%d", n);
	return (0);
}

